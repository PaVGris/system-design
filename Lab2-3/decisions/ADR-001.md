# API Gateway как единая точка входа

| Metadata | Value |
|----------|-------|
| Date | 2026-03-20 |
| Author | @PaVGris |
| Status | Approved |
| Tags | architecture, gateway, auth |

## Контекст и мотивация

Система состоит из трёх сервисов: user-service, item-service, cart-service. Нужно решить как клиент будет с ними взаимодействовать и где проверять аутентификацию.

## Решение

Добавить api-gateway на порту 8080, как единственную точку входа для клиента.

Gateway выполняет две функции:

**1. Маршрутизация:**
```
/users/* в user-service:8081
/items/* в item-service:8082
/cart/*  в cart-service:8083
```

**2. Проверка токена перед каждым запросом:**
```
клиент -> gateway -> GET /users/validate (токен валиден?) ---(да)--> целевой сервис
```

Публичные endpoints (`/users/register`, `/users/login`) не требуют токена.

## Причины

Без gateway каждый сервис должен сам проверять токен, а это дублирование логики в трёх местах.

С gateway проверка токена централизована. Сервисы доверяют заголовку `X-User-Id` который передаёт gateway — они не знают ничего о токенах

## Ограничения

- Если умрет Gateway, то не будет работать абсолютно всё
- Добавляет один лишний HTTP запрос к user-service при каждом запросе (валидация токена)

## Что изменится при переходе на JWT

Gateway сможет проверять токен локально без запроса к user-service, так как убирает лишние действие.